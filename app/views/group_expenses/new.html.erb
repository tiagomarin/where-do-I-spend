<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New group expense</h1>

  <%= render "form", group_expense: @group_expense %>

  <%= link_to 'Back to group expenses', user_group_group_expenses_path(current_user, @group, @group_expense), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
